# CLAUDE.md

## 概要

Nix Flakes で管理する dotfiles。システム設定とユーザー環境設定を分離し、ネイティブ NixOS と WSL に対応する。ユーザー環境側の設定は他 OS（Nix-as-PM）でも再利用できるようポータブルに保つ。

## 設計方針

**os/ と home/ の分離:** `os/` に置くのはシステム権限が必要なもの・OS 固有のものだけに限る（ブート、ハードウェア、デーモン、ログインシェルの登録など）。プログラムの設定内容は `home/` に置き、他 OS でも再利用できるようにする。

**追加先は home/ が既定:** パッケージも設定も、迷ったら `home/` に入れる。`os/` に入れるのは、システム全体で必要な場合か、`home/` では実現できない場合のみ。

**設定は可能な限り Nix 式で書く:** モジュールが構造化オプションを提供しているなら必ずそれを使い、対象言語の生文字列を埋め込むオプションで代替しない。生文字列は Nix の型検査を受けられず、LSP もシンタックスハイライトも効かないため腐敗しやすい。対応するオプションが存在せず生文字列を書くしかない場合は、実装せずユーザーに相談すること。
